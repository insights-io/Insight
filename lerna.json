{
  "packages": ["frontend/**"],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.md", "**/test/**", "**/*.spec.ts", "**/*.e2e.ts"],
      "allowBranch": ["master"]
    }
  }
}

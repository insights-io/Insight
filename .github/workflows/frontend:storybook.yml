---
on:
  push:
    branches: [master]
    paths:
      - '.github/workflows/frontend:storybook.yml'
      - 'frontend/**'
      - 'lerna.json'
      - 'package.json'
      - 'tsconfig.json'
      - 'yarn.lock'

env:
  CI: true

jobs:
  pr-deploy-storybook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"
      - uses: actions/cache@v2
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - name: Install & build 🏗️
        run: |
          yarn install --frozen-lockfile
          yarn workspace @rebrowse/types build
          yarn concurrently "yarn workspace @rebrowse/storybook build && yarn workspace @rebrowse/next-storybook build" "yarn workspace @rebrowse/sdk build" "yarn workspace @rebrowse/elements build"
          yarn concurrently "yarn workspace @rebrowse/app storybook:build" "yarn workspace @rebrowse/try storybook:build" "yarn workspace @rebrowse/storybook storybook:build" "yarn workspace @rebrowse/homepage storybook:build"
      - uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          FOLDER: frontend/app/storybook-static
          TARGET_FOLDER: app/storybook/master
      - uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          FOLDER: frontend/try/storybook-static
          TARGET_FOLDER: try/storybook/master
      - uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          FOLDER: frontend/homepage/storybook-static
          TARGET_FOLDER: homepage/storybook/master
      - uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          FOLDER: frontend/shared/storybook/storybook-static
